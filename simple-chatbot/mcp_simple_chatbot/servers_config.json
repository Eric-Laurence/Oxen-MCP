{
  "mcpServers": {
    "oxen-search": {
      "command": "uv",
      "args": ["run", "mcp-oxen-search", "--repo", "username/repository", "--dataframe", "data.parquet"],
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
}